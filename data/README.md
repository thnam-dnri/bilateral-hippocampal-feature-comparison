# Data

`full_dataset_111patients.csv` is the subject-level input table used by the analysis script.

Columns include:

- `Subject`
- bilateral morphometry features encoded as `*_Left` and `*_Right`
- `Label`

This file is the direct input used by `analysis/run_feature_engineering_comparison.py`.
